# Cursor Rules 配置文件

## 通用规则
这是一个精简版 RTK 基站项目。优先使用 pyserial 进行串口通信。代码要求极致简洁，模块化，不使用大型框架

### 代码风格
- 优先使用现代 JavaScript/TypeScript 特性（ES6+）
- 使用有意义的变量和函数名
- 保持函数简洁，单一职责
- 添加必要的注释，特别是复杂逻辑
- 遵循项目现有的代码风格和约定

### 代码质量
- 编写可读、可维护的代码
- 避免重复代码（DRY 原则）
- 优先使用类型安全的代码（TypeScript）
- 处理错误情况，提供有意义的错误信息
- 编写自文档化的代码

### 最佳实践
- 使用 async/await 而不是 Promise.then()
- 优先使用 const，必要时使用 let，避免 var
- 使用箭头函数保持上下文一致性
- 使用解构赋值简化代码
- 使用模板字符串而不是字符串拼接

### 文件组织
- 保持文件结构清晰有序
- 相关功能组织在同一目录
- 使用有意义的文件名
- 避免创建过大的文件（建议单个文件不超过 500 行）

### 测试
- 编写可测试的代码
- 为关键功能添加测试
- 保持测试代码简洁清晰

### 性能
- 避免不必要的计算和循环
- 优化数据库查询
- 使用适当的数据结构和算法
- 考虑内存使用和性能影响

### 安全性
- 验证和清理用户输入
- 避免 SQL 注入、XSS 等安全漏洞
- 使用安全的认证和授权机制
- 不要在代码中硬编码敏感信息

### Git 提交
- 编写清晰的提交信息
- 保持提交小而专注
- 遵循项目的 Git 工作流

### 响应式设计（如适用）
- 确保代码在不同设备上正常工作
- 考虑移动端和桌面端的体验
- 使用响应式设计原则

## 项目特定规则

# 根据项目需求添加特定规则
# 例如：
# - 使用特定的框架或库
# - 遵循特定的架构模式
# - 使用特定的命名约定

